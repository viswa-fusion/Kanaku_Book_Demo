<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".presentation.KanakuBookApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".presentation.activity.AppEntryPoint"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.activity.MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" />
        <activity android:name=".presentation.activity.ProfileActivity" />
        <activity android:name=".presentation.activity.AddFriendActivity" />
        <activity
            android:name=".presentation.activity.AddGroupActivity"
            android:windowSoftInputMode="adjustResize|stateUnchanged" />
        <activity
            android:name=".presentation.activity.FriendDetailPageActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".presentation.activity.GroupDetailPageActivity"
            android:launchMode="singleTask" />
        <activity android:name=".presentation.activity.AddExpenseActivity" />
        <activity
            android:name=".presentation.activity.EditProfileActivity"
            android:windowSoftInputMode="adjustResize|stateUnchanged" />
        <activity android:name=".presentation.activity.GroupProfilePageActivity" />
        <activity android:name=".presentation.activity.ExpenseDetailActivity" />
        <activity android:name=".presentation.activity.FriendProfilePageActivity" />
        <activity android:name=".presentation.activity.SelectSplitWithActivity" />
    </application>

</manifest>